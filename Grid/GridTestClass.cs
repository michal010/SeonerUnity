using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GridTestClass : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        //Seoner.Grid testGrid = new Seoner.Grid<TileGridObject>(4, 2, 1f,Vector3.zero, (Seoner.Grid<TileGridObject> g, int x, int y) => new );
    }
}